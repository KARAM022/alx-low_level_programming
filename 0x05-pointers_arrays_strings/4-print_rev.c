#include <stdio.h>
#include "main.h"

/**
 * print_rev - hh
 * @s: ff
 * Return: nothing
*/

void print_rev(char *s)
{
	int len = _strlen(*s),i;

	for (i = len; i = -1; i--)
	{
		putchar(s[i]);
	}
	putchar('\n');
}
