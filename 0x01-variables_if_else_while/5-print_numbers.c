#include <stdio.h>

/**
 * main - Determines if a number is positive. negative or zero.
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
    int d = 0;
    while (d < 10)
    {
        printf("%d",d);
        d++;
    }
    putchar('\n');
    return (0);
}