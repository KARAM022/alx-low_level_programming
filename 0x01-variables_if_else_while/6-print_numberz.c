#include <stdio.h>

/**
 * main - Determines if a number is positive. negative or zero.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int d = 48;

	while (d < 58)
	{
		putchar(d);
		d++;
	}
	putchar('\n');
	return (0);
}
